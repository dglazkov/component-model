<element extends="table" name="x-table-chart" constructor="TableChart">
    <script>

        this.addEventListener('create', function(evt) {
            this._canvas = evt.shadowRoot.firstElementChild;
            this.draw();
        });

        this.addEventListener('insert', function() { this._visible = true; });
        this.addEventListener('remove', function() { this._visible = false; });

        TableChart.prototype.draw = function()
        {
            if (!this._visible)
                return;

            var type = this.chartType;
            var color = this.chartColor;
            var canvas = this._canvas;

            // Read the table data and draw the graph on canvas.
            // ...

            this.dispatchEvent(new Event('draw'));
        }

        this.reflectAttribute('x-chart-type', 'chartType', TableChart.prototype.draw);
        this.reflectAttribute('x-chart-color', 'chartColor', TableChart.prototype.draw);
        this.setupEvent('draw');


    </script>
    <template>
        <canvas></canvas>
    </template>
</element>